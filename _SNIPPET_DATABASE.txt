"""
COPIAR E COLAR ESTE CÓDIGO EXATAMENTE
==========================================

PASSO 1: database.py
--------------------
Abra database.py e encontre a linha 183 que diz:
    # Criar usuário admin padrão se não existir

COLE ESTAS 5 LINHAS LOGO ACIMA (entre a linha 182 e 183):

            # Inicializar tabela de tokens públicos
            try:
                import token_manager
                token_manager.inicializar_tabela_tokens()
            except Exception as e:
                logger.warning(f"Erro ao inicializar tokens públicos: {e}")
            
"""

# NÃO USE ESTE ARQUIVO! É APENAS REFERÊNCIA VISUAL
# Abra database.py e adicione manualmente no local indicado acima
